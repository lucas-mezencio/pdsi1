DATABASE_URL=postgres://mednotify:mednotify@localhost:5432/mednotify?sslmode=disable
TEST_DATABASE_URL=postgres://mednotify:mednotify@localhost:5432/mednotify?sslmode=disable
HTTP_ADDR=:8080
REDIS_ADDR=localhost:6379

# notifier mode: dev uses dummy sender, ready uses firebase
NOTIFIER_MODE=dev

# firebase service account json path
FIREBASE_CREDENTIALS_FILE=/path/to/firebase-service-account.json
