version: "2"
linters:
  settings:
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (*database/sql.Rows).Close
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
